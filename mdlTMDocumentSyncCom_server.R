
mdlTMDocumentSyncServer::saleOutServer(input = input,output = output,session = session,dms_token = dms_token)
#完成日期组件注册
mdlTMDocumentSyncServer::saleReturnServer(input = input,output = output,session = session,dms_token = dms_token)


mdlTMDocumentSyncServer::purchaseInboundServer(input = input,output = output,session = session,dms_token = dms_token)
#完成日期组件注册
mdlTMDocumentSyncServer::purchaseReturnServer(input = input,output = output,session = session,dms_token = dms_token)


  

