
mdlTMDocumentSyncServer::saleOutServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
#完成日期组件注册
mdlTMDocumentSyncServer::saleReturnServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)


mdlTMDocumentSyncServer::purchaseInboundServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
#完成日期组件注册
mdlTMDocumentSyncServer::purchaseReturnServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)

mdlTMDocumentSyncServer::productWarehousingServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::prd_pickmtrServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_miscellaneous_wasteServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_miscellaneous_crushServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_misdelivery_crushServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_dismantlingServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_assemblyServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_misdelivery_watsteServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_miscellaneous_salServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)

mdlTMDocumentSyncServer::stk_misdelivery_salServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_miscellaneous_stdServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_misdelivery_stdServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)

mdlTMDocumentSyncServer::prd_pickmtrl_wasteServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::rpd_returnstockServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)

mdlTMDocumentSyncServer::stk_stktransferin_stdServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)

mdlTMDocumentSyncServer::stk_stktransferin_materialServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
mdlTMDocumentSyncServer::stk_stktransferin_othersServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)

# mdlTMDocumentSyncServer::directTransferServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
# 
# mdlTMDocumentSyncServer::otherInboundServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
# 
# mdlTMDocumentSyncServer::otherOutboundServer(input = input,output = output,session = session,dms_token = dms_token,wms_token = wms_token)
